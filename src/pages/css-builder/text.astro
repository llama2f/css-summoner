---
/**
 * Text コンポーネント ドキュメント
 *
 * このファイルはスクリプトによって自動生成されました。
 * 元のCSSファイルのアノテーション、または astro-generator.js を編集してください。
 * ソース: text/variants.css, text/base.css
 */
import Layout from '@layouts/Layout.astro'; // 正しいエイリアスを使用
import Menu from '@/css-builder/layouts/Menu.astro'; // css-builder内のMenuを使用

// スタイルシートのインポート (必要に応じて調整)
// import '@/css-builder/styles/styles.css'; // 全体スタイル
// import '@/css-builder/styles/text/index.css'; // 個別スタイル (必要なら)

const pageTitle = "Text コンポーネント";
const pageDesc = "Text コンポーネントのスタイルバリエーションと使用例。";
---

<Layout pageTitle={pageTitle} desc={pageDesc}>
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-4">{pageTitle}</h1>
    <p class="text-lg text-neutral-600 mb-6">
      テキストデコレーションの基本スタイルを提供します。
      {baseClassName && <code class="ml-2 text-sm bg-neutral-200 p-1 rounded">.text-deco-base</code>}
    </p>
    <div class="mb-6">
      <a href="/css-builder" class="text-primary hover:underline">
        ← クラスビルダーに戻る
      </a>
    </div>
    <Menu client:load /> {/* client:load を追加 */}

    {variants.length > 0 && (
      <section class="mb-12">
        <h2 class="text-2xl font-semibold mb-6 border-b pb-2">バリアント</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          
          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Highlight</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-highlight</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、微妙な背景ハイライトを適用します。marker-fullに似ていますが、異なるスタイル設定が可能です。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Marker</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-marker</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、テキスト背景の下部に蛍光ペン効果を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Marker Double</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-marker-double</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、二重線のマーカー効果を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Marker Full</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-marker-full</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、テキスト背景全体に蛍光ペン効果を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Marker Stripe</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-marker-stripe</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、テキスト下部にストライプ状のマーカー効果を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Quote</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-quote</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、テキスト（通常は引用）に左ボーダーを適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Underline</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-underline</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、標準的な実線の下線を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Underline Dashed</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-underline-dashed</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、破線の下線を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Underline Dotted</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-underline-dotted</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、点線の下線を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Underline Thick</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-underline-thick</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、太い実線の下線を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          

          <div class="border rounded-lg shadow-sm overflow-hidden">
            <div class="p-4 bg-neutral-100">
              <h3 class="text-lg font-medium">Underline Wavy</h3>
              <code class="block text-sm text-neutral-700 mt-1">.text-underline-wavy</code>
            </div>
            <div class="p-4">
              <p class="text-sm text-neutral-600 mb-4">コンポーネントカラーを使用して、波線の下線を適用します。</p>
              <div class="bg-white p-6 rounded border border-dashed flex items-center justify-center min-h-[100px]">
                <!-- HTMLをエスケープせずに埋め込む -->
                <Fragment set:html={docGenerator.getComponentPreviewHtml(componentType, v.className, baseClasses)} />
              </div>
            </div>
          </div>
          
        </div>
      </section>
    )}

    <section class="mb-12">
      <h2 class="text-2xl font-semibold mb-6 border-b pb-2">使用例</h2>
      <div class="bg-neutral-800 text-neutral-100 p-4 rounded-lg overflow-x-auto">
        <pre class="text-sm">
          <!-- HTMLをエスケープせずに埋め込む -->
          <Fragment set:html={docGenerator.getComponentExampleCode(componentType, componentExamples, baseClasses, componentVariants)} />
        </pre>
      </div>
    </section>

    <Menu client:load /> {/* client:load を追加 */}
    <div class="mt-6">
      <a href="/css-builder" class="text-primary hover:underline">
        ← クラスビルダーに戻る
      </a>
    </div>
  </div>
</Layout>
